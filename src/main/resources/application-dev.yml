spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/billwise_dev
    username: billwise
    password: billwise
  jpa:
    show-sql: true
  flyway:
    clean-on-validation-error: true

logging:
  level:
    sys.azentic.billwise_api: DEBUG
    org.springframework.security: DEBUG
